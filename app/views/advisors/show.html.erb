<div class="main advisor-show">
  <div class="container">
    <div class="advisor">
      <h2><%= @advisor.name %></h2>
      <p><%= @advisor.email %></p>
      <% if @advisor.id %>
        <%= link_to("編集", "/advisors/#{@advisor.id}/edit") %>
      <% end %>
    </div>
  </div>
</div>